{
  "name": "generate-url-for-mr",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "assr": "deno run --allow-run ./index.ts assr",
    "akit": "deno run --allow-run ./index.ts akit",
    "create-mr": "deno run --allow-run --allow-net ./create-mr.ts"
  }
}
